<!-- final-cta -->
<hr style="margin:0;">
<section class="t-c call-to-action" id="{{ ctaSlug | slugify or 'final-cta' }}" aria-labelledby="cta-heading">
  <div class="container">
    <h2 id="cta-heading">{{ ctaHeading }}</h2>
    <p>{{ ctaText | safe }}</p>
    
    {% if ctaTrust %}
    <div class="cta-trust">
      {% for badge in ctaTrust %}
        <span class="trust-item"><span class="check" aria-hidden="true">✓</span> {{ badge }}</span>
      {% endfor %}
    </div>
    {% endif %}     
    
    <a href="tel:+19786694919" class="cta-phone">978-669-4919</a>
  </div>
</section>

{# 
<!--final-cta-->
<hr style="margin:0;">
<section class="t-c call-to-action" id="{{ ctaSlug | slugify or 'final-cta'}}"  aria-labelledby="cta-heading" >
    <div class="container">
        <h2>{{ ctaHeading }}</h2>
        <p>{{ ctaText | safe }}</p>
        {% if ctaTrust %}
        <div class="cta-trust c-b">
            {% for badge in ctaTrust %}
            <span>✓ {{ badge }}</span>
            {% endfor %}
        </div>
        {% endif %}     
        <a href="tel:+19786694919">978-669-4919</a></div>
    </div>
</section> #}