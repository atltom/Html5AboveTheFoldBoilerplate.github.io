{# 
Service-specific pages
For individual service pages (e.g., Masonry, Paving), you might want a Service schema. In base.njk, create a Nunjucks {% block schema %} hook so individual pages can inject custom JSON-LD if needed.
 #}